[pytest]
testpaths = tests
pythonpath = src
addopts = -q --strict-markers
markers =
    unit: pure domain / fast
    integration: db or api

[coverage:run]
branch = True
source = mytimetree

[coverage:report]
show_missing = True
skip_empty = True
fail_under = 70
